name: Hola Python
on: 
  push:
    branches: [ main, dev]
       

jobs:
  build_3:
  
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Python Tests 
        run: | 
         python --version
         python test.py
      
